{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app.js && npm run dev ../gui/GeoQuizzer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "blåsipporna",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
